// constants
const addBook = document.getElementById("add-book");
const bookStorage = append("./bookStorage.json"); // don't know if it will work yet

// storing book in local storage( in bookStorage.json )
